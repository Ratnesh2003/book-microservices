spring:
    data:
        mongodb:
            uri: mongodb://localhost:27017/food-service
    application:
        name: book-service

eureka:
    client:
        service-url:
            defaultZone: http://eureka:password@localhost:8761/eureka/
server:
    port: 0
